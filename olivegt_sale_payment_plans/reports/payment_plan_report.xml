<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data>
    <!-- Acción de reporte -->
    <record id="action_report_payment_plan" model="ir.actions.report">
      <field name="name">Payment Plan</field>
      <field name="model">payment.plan</field>
      <field name="report_type">qweb-pdf</field>
      <field name="report_name">olivegt_sale_payment_plans.report_payment_plan</field>
      <field name="report_file">olivegt_sale_payment_plans.report_payment_plan</field>
      <field name="binding_model_id" ref="model_payment_plan"/>
      <field name="binding_type">report</field>
    </record>

    <!-- Template QWeb -->
    <template id="report_payment_plan">
      <t t-call="web.html_container">
        <t t-foreach="docs" t-as="o">
          <t t-call="web.external_layout">
            <style>
              /* Base */
              .page { font-family:'Lato','Helvetica',sans-serif; font-size:8px; line-height:1.2; }
              .report-title    { text-align:center; font-size:12px; font-weight:600; margin-bottom:4px; }
              .report-subtitle { text-align:center; font-size:8px; color:#555; margin-bottom:8px; }
              .section-title   { font-size:9px; font-weight:600; margin:12px 0 4px; }
              .footer-note     { text-align:center; font-size:7px; color:#888; margin-top:10px; }

              /* Cabecera */
              .summary-table { width:100%; border-collapse: collapse; margin-bottom:8px; }
              .summary-table td { padding:2px 4px; vertical-align:top; }
              .summary-table strong { font-weight:600; color:#333; }

              /* Cuotas */
              .table-condensed {
                width:100%; border-collapse: separate; border-spacing:0;
                border:1px solid #ccc; border-radius:8px; overflow:hidden;
              }
              .table-condensed thead { background:#2c3e50; color:#fff; }
              .table-condensed thead th {
                padding:8px; font-weight:600; text-align:center;
              }
              .table-condensed tbody tr:nth-child(odd)  { background:#ecf0f1; }
              .table-condensed tbody tr:nth-child(even) { background:#fff; }
              .table-condensed tbody td {
                padding:8px; border-bottom:1px solid #ddd; vertical-align:middle;
              }

              /* Redondear esquinas del encabezado */
                .table-condensed thead th:first-child {
                border-top-left-radius: 8px;
                }
                .table-condensed thead th:last-child {
                border-top-right-radius: 8px;
                }

                /* Redondear esquinas de la última fila */
                .table-condensed tbody tr:last-child td:first-child {
                border-bottom-left-radius: 8px;
                }
                .table-condensed tbody tr:last-child td:last-child {
                border-bottom-right-radius: 8px;
                }

              /* Progreso */
              .progress-container {
                background:#eee; height:6px; border-radius:3px;
                overflow:hidden; margin-top:4px;
              }
              .progress-bar { background:#2c3e50; height:6px; }

              /* Pagos – nuevo diseño */
              .payments-container {
                display:flex; flex-direction:column; gap:4px;
              }
              .payment-group {
                display:inline-flex;
                padding: 5px 4px;
                background:#2c3e50;
                border-radius:3px;
                overflow:hidden;
                margin-bottom:2px;
                color:#fff;
              }
              .payment-group span {
                padding:6px 8px;
                font-size:7px;
                white-space:nowrap;
              }
              .payment-amount { flex:2; text-align:left; font-weight:600; }
              .payment-date   { flex:1; text-align:center; }
              .payment-ref    { flex:3; text-align:right; overflow:hidden; text-overflow:ellipsis; }

              /* Utilidades */
              .text-center { text-align:center; }
              .text-right  { text-align:right; }
            </style>

            <div class="page">
              <!-- Título -->
              <div class="report-title">ESTADO DE CUENTA</div>
              <div class="report-subtitle">Plan de Pagos: <span t-field="o.name"/></div>

              <!-- Resumen -->
              <table class="summary-table">
                <tr>
                  <td><strong>Cliente:</strong><br/><span t-field="o.partner_id"/></td>
                  <td><strong>Orden:</strong><br/><span t-field="o.sale_id.name"/></td>
                  <td><strong>Fecha:</strong><br/>
                    <span t-field="o.date" t-options='{"format":"dd/MM/yyyy"}'/>
                  </td>
                </tr>
                <tr>
                  <td><strong>Precio:</strong><br/>
                    <span t-field="o.sale_id.amount_untaxed"
                          t-options='{"widget":"monetary","display_currency":o.currency_id}'/>
                  </td>
                  <td><strong>Total:</strong><br/>
                    <span t-field="o.total_amount"
                          t-options='{"widget":"monetary","display_currency":o.currency_id}'/>
                  </td>
                  <td><strong>Saldo:</strong><br/>
                    <span t-field="o.amount_residual"
                          t-options='{"widget":"monetary","display_currency":o.currency_id}'/>
                  </td>
                </tr>
              </table>

              <!-- Cuotas -->
              <div class="section-title">DETALLE DE CUOTAS</div>
              <table class="table-condensed">
                <thead>
                  <tr>
                    <th>Cuota</th><th>Vence</th><th>Monto</th><th>Int.</th>
                    <th>Total</th><th>Asig.</th><th>Saldo</th><th>Pagos</th>
                  </tr>
                </thead>
                <tbody>
                  <t t-set="idx" t-value="0"/>
                  <t t-foreach="o.line_ids.sorted(key=lambda l: l.date)" t-as="line">
                    <t t-set="idx" t-value="idx + 1"/>
                    <tr>
                      <td class="text-center">
                        <t t-esc="line.name or 'Cuota ' + str(idx)"/>
                      </td>
                      <td class="text-center">
                        <span t-field="line.date" t-options='{"format":"dd/MM/yyyy"}'/>
                      </td>
                      <td class="text-right">
                        <span t-field="line.amount"
                              t-options='{"widget":"monetary","display_currency":o.currency_id}'/>
                      </td>
                      <td class="text-right">
                        <t t-if="line.interest_amount &gt; 0">
                          <span t-field="line.interest_amount"
                                t-options='{"widget":"monetary","display_currency":o.currency_id}'/>
                        </t>
                      </td>
                      <td class="text-right">
                        <span t-field="line.total_with_interest"
                              t-options='{"widget":"monetary","display_currency":o.currency_id}'/>
                      </td>
                      <td class="text-right">
                        <span t-field="line.allocated_amount"
                              t-options='{"widget":"monetary","display_currency":o.currency_id}'/>
                        <div class="progress-container">
                          <div class="progress-bar"
                               t-att-style="'width: {}%;'.format(int(((line.allocated_amount or 0.0) / ((line.total_with_interest or line.amount) or 1.0)) * 100))"/>
                        </div>
                      </td>
                      <td class="text-right">
                        <t t-esc="(line.total_with_interest or line.amount) - (line.allocated_amount or 0.0)"/>
                      </td>
                      <td>
                        <t t-if="line.reconciliation_ids.filtered(lambda r: r.state=='confirmed')">
                          <div class="payments-container">
                            <t t-foreach="line.reconciliation_ids.filtered(lambda r: r.state=='confirmed')" t-as="rec">
                              <div class="payment-group">
                                <span class="payment-amount">
                                  <span t-field="rec.amount"
                                        t-options='{"widget":"monetary","display_currency":o.currency_id}'/>
                                </span>
                                <span class="payment-date">
                                  <span t-field="rec.date" t-options='{"format":"dd/MM/yyyy"}'/>
                                </span>
                                <span class="payment-ref" title="&lt;span t-esc=&quot;rec.move_payment_reference or rec.move_id.name&quot;/&gt;">
                                  <t t-esc="rec.move_payment_reference or rec.move_id.name"/>
                                </span>
                              </div>
                            </t>
                          </div>
                        </t>
                      </td>
                    </tr>
                  </t>
                </tbody>
              </table>

              <!-- Pie -->
              <div class="footer-note">
                <p>Documento informativo, no representa comprobante fiscal.</p>
                <p>Página 1/1</p>
              </div>
            </div>
          </t>
        </t>
      </t>
    </template>
  </data>
</odoo>
